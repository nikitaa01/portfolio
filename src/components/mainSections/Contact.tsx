import sections from "@/constants/sections"
import SectionBox from "@/components/SectionBox"

export default function Contact() {
    return (
        <SectionBox id={sections.contact.id}>
            <h1>Contact</h1>
        </SectionBox>
    )
}