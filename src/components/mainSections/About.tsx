import sections from "@/constants/sections"
import SectionBox from "@/components/SectionBox"

export default function About() {
    return (
        <SectionBox id={sections.about.id}>
        </SectionBox>
    )
}